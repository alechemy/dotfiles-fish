yaml name: slack
# disable default.yml expansions in Slack, which has its own `:` shortcuts
filter_title: 'com.tinyspeck.slackmacgap'
exclude_default_entries: true

matches:
  # teh -> the
  - trigger: 'teh'
    replace: 'the'
    word: true
