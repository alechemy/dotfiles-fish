#!/usr/bin/env fish

git rev-parse --git-dir >/dev/null 2>&1
and if git show-ref --verify --quiet refs/remotes/origin/main
    echo main
else if git show-ref --verify --quiet refs/heads/main
    echo main
else
    echo master
end
