# Source-of-truth template. After cloning, copy to `env` and fill in real values.
# Loaded by stow/sketchybar/.config/sketchybar/plugins/feishin.sh and any other
# script that needs to talk to Navidrome.
NAVIDROME_URL="http://CHANGEME:4533"
NAVIDROME_USERNAME="CHANGEME"
