# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# Config version for compatibility and deprecations
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = [
    'exec-and-forget sketchybar',
    'exec-and-forget borders',
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Notify SketchyBar about workspace changes
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config version is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = ["1", "2", "3", "4", "5", "6"]

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = [
    "exec-and-forget bash -c 'if [ \"$(aerospace list-modes --current)\" = \"service\" ]; then borders active_color=0xffff4444 inactive_color=0xffff4444; else borders active_color=0xffe2e2e3 inactive_color=0xff414550; fi'"
]

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = 8
    inner.vertical =   8
    outer.left = [{ monitor.main = 600 }, 8]
    outer.right = [{ monitor.main = 600 }, 8]
    outer.bottom =     8
    outer.top =        48

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]
    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    cmd-alt-ctrl-shift-leftSquareBracket = 'layout tiles horizontal vertical'
    cmd-alt-ctrl-shift-rightSquareBracket = 'layout accordion horizontal vertical'

    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    cmd-alt-ctrl-shift-h = 'focus left'
    cmd-alt-ctrl-shift-j = 'focus down'
    cmd-alt-ctrl-shift-k = 'focus up'
    cmd-alt-ctrl-shift-l = 'focus right'
    cmd-alt-ctrl-shift-w = 'close'
    cmd-alt-ctrl-shift-backtick = 'exec-and-forget bash -c "$HOME/.dotfiles/scripts/aerospace-cycle-window.sh next"'
    ctrl-alt-shift-backtick = 'exec-and-forget bash -c "$HOME/.dotfiles/scripts/aerospace-cycle-window.sh prev"'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    cmd-alt-ctrl-shift-left  = 'move left'
    cmd-alt-ctrl-shift-down  = 'move down'
    cmd-alt-ctrl-shift-up    = 'move up'
    cmd-alt-ctrl-shift-right = 'move right'
    cmd-alt-ctrl-shift-space = 'fullscreen'

    # See: https://nikitabobko.github.io/AeroSpace/commands#resize
    # cmd-alt-ctrl-shift-minus = 'resize smart -50'
    # cmd-alt-ctrl-shift-equal = 'resize smart +50'

    # Cycle outer gap presets (full → medium → narrow) on ultrawide
    cmd-alt-ctrl-shift-g = 'exec-and-forget bash $HOME/.dotfiles/scripts/aerospace-cycle-gaps.sh'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    # Hyper (CapsLock) + number = switch workspace
    cmd-alt-ctrl-shift-1 = 'workspace 1'
    cmd-alt-ctrl-shift-2 = 'workspace 2'
    cmd-alt-ctrl-shift-3 = 'workspace 3'
    cmd-alt-ctrl-shift-4 = 'workspace 4'
    cmd-alt-ctrl-shift-5 = 'workspace 5'
    cmd-alt-ctrl-shift-6 = 'workspace 6'
    # cmd-alt-ctrl-shift-7 = 'workspace 7'
    # cmd-alt-ctrl-shift-8 = 'workspace 8'
    # cmd-alt-ctrl-shift-9 = 'workspace 9'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    # Shyper (CapsLock + left cmd) + number = move window to workspace (via Karabiner remap)
    ctrl-alt-shift-1 = 'move-node-to-workspace 1'
    ctrl-alt-shift-2 = 'move-node-to-workspace 2'
    ctrl-alt-shift-3 = 'move-node-to-workspace 3'
    ctrl-alt-shift-4 = 'move-node-to-workspace 4'
    ctrl-alt-shift-5 = 'move-node-to-workspace 5'
    ctrl-alt-shift-6 = 'move-node-to-workspace 6'
    # ctrl-alt-shift-7 = 'move-node-to-workspace 7'
    # ctrl-alt-shift-8 = 'move-node-to-workspace 8'
    # ctrl-alt-shift-9 = 'move-node-to-workspace 9'

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    cmd-alt-ctrl-shift-tab = 'workspace-back-and-forth'

    # Shyper+tab = move focused window to the last workspace (and follow it)
    ctrl-alt-shift-tab = 'exec-and-forget bash -c "aerospace workspace-back-and-forth && prev=\$(aerospace list-workspaces --focused) && aerospace workspace-back-and-forth && aerospace move-node-to-workspace --focus-follows-window \$prev"'

    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    cmd-alt-ctrl-shift-semicolon = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    0 = ['exec-and-forget fish -c reload_wm', 'mode main']
    b = ['balance-sizes', 'mode main']
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    h = ['join-with left', 'mode main']
    j = ['join-with down', 'mode main']
    k = ['join-with up', 'mode main']
    l = ['join-with right', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    backspace = ['close-all-windows-but-current', 'mode main']
    esc       = 'mode main'
    semicolon = 'mode main'

# Workspace Assignments
[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-id = 'com.vscodium'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-id = 'dev.zed.Zed'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-id = 'dev.zed.Zed-Preview'
run = 'move-node-to-workspace 1'

[[on-window-detected]]
if.app-name-regex-substring = 'Teams'
run = 'move-node-to-workspace 2'

[[on-window-detected]]
if.app-id = 'com.apple.Safari.WebApp.5353ED03-6BF4-40DB-A16D-2146FB4CD7A3'
run = 'move-node-to-workspace 2'

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-id = 'com.apple.mail'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-id = 'ru.keepcoder.Telegram'
run = 'move-node-to-workspace 3'

[[on-window-detected]]
if.app-id = 'com.apple.Safari'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-id = 'org.chromium.Chromium'
run = 'move-node-to-workspace 4'

[[on-window-detected]]
if.app-id = 'com.devon-technologies.think'
run = 'move-node-to-workspace 5'

[[on-window-detected]]
if.app-id = 'com.culturedcode.ThingsMac'
run = 'move-node-to-workspace 5'

[[on-window-detected]]
if.app-name-regex-substring = 'Qobuz'
run = 'move-node-to-workspace 6'

[[on-window-detected]]
if.app-name-regex-substring = 'Feishin'
run = 'move-node-to-workspace 6'

[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = 'move-node-to-workspace 6'

# Floating Windows
[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.agiletortoise.Drafts-OSX'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.aone.keka'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.clickontyler.Ears'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.firecore.infuse'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.runningwithcrayons.Alfred-Preferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.sindresorhus.Aiko'
run = 'layout floating'
